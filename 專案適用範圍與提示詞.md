# 這套系統適合放哪些專案，以及開新專案時怎麼跟 AI 說

這份文件分兩層：

- **前半「概要」給完全不懂技術的人看** —— 沒有數字、沒有術語，看完就能自己判斷
- **後半「詳細說明」給有底子的人看** —— 遇到模糊案例時翻這裡

---

# 概要

## 一、這套系統適合哪些專案

### 問自己一個問題就好

> **這個東西做好之後，我會怎麼交給學生？**

| 你的答案 | 這套系統能做什麼 |
|---|---|
| 「我傳一個**網址**給他，他點開就能用」 | ✅ **做得到**，這就是它的本業 |
| 「我傳一個**檔案**給他，他要先安裝才能用」 | 🔶 程式本身做不到，但可以幫你做一個**下載頁**：學生點網址進去，看到說明和下載按鈕 |
| 「我傳一段**影片**給他」 | ❌ 影片放 YouTube，再把連結加進展示中心 |

就這樣。不用懂技術名詞，這三句話就能分辨。

### 舉例

**✅ 點開網址就能用的（做得到）**

- 練習題、線上測驗、搶答遊戲
- 互動教學工具（電阻色碼練習、抽籤程式、連連看）
- 班級網頁、課程公告、教材說明頁
- 報名表、投票、課堂留言板
- 讓學生上傳自己的作業檔，網頁馬上檢查給他看

**🔶 要先安裝才能用的（幫你做下載頁）**

- 桌面小工具（PDF 處理工具、影片裁剪工具）
- 瀏覽器外掛
- 要去 YouTube 抓影片的工具

> 「影片裁剪工具」就是這樣上線的：展示中心上放的是一張說明頁加一個下載按鈕，
> 程式本身是壓縮檔。卡片會自動標「需下載安裝」，學生一看就知道。

**❌ 不適合放這裡的**

- 影片 → 放 YouTube
- 很多份大的 PDF 教材 → 放學校的雲端硬碟，展示中心只放連結

### 一個模糊地帶，直接問 AI 就好

有些工具**看起來要安裝，其實可以改成網頁**。分界在於：

> 這個程式處理的，是**學生自己丟給它的檔案**，還是**要去電腦裡翻東西**？

- 學生自己選檔案丟進去 → **通常可以改成網頁**
- 程式要自己去翻資料夾、或要去別的網站抓東西 → 不行

不確定的時候，把資料夾丟給 AI 問：「這個能不能改成網頁版？」

---

## 二、開新專案時貼給 AI 的話

**直接複製這一整段**，只要把「⟨⟩」裡面換成你要做的東西：

> 我想做一個 **⟨在這裡寫你想做的東西⟩**。
>
> 做好之後我要放到網路上給學生用，用的是 Cloudflare 的免費方案。請照這幾點做：
>
> 1. **先告訴我做不做得到。** 如果這個東西必須先安裝在電腦上才能用、
>    或是要去別的網站抓資料、或是要處理影片，就直接說做不到，
>    我會改用別的方式。**不要硬做一個半殘的版本。**
> 2. 做得到的話，**全部用網頁做**（HTML／CSS／JavaScript），
>    不要用 Python 或其他需要另外安裝的東西。
> 3. 網頁檔案**全部放在一個叫 `public` 的資料夾裡**，首頁一定要叫 `index.html`。
> 4. 有圖片的話，先幫我壓小一點再放進去。
> 5. 做完之後，**幫我把首頁截一張圖，放在最外層**（不要放進 `public` 裡面）。
> 6. 如果我給你的東西裡面有學生名單、密碼、個人資料，**提醒我**，不要放進去。

**只有第 1 點需要你動腦**——把「⟨⟩」換掉。其他五點原封不動貼過去就好。

### 為什麼第 1 點放最前面

因為最貴的錯誤是**做完了才發現放不上去**。讓 AI 先講「做不做得到」，
你花三十秒就知道要不要繼續，而不是做了兩天才卡住。

---

## 三、放上去的東西，別人看得到什麼

**這一節很重要，而且答案可能跟你想的不一樣。**

### 網頁的程式碼，別人一定看得到

只要是「打開網址就能用」的東西，**它的程式碼一定會被下載到對方的電腦上**——
這不是設定問題，而是網頁運作的方式：瀏覽器要能執行它，就必須先拿到它。

所以對方只要按 F12、或按右鍵看原始碼，就看得到你的 HTML、CSS 和 JavaScript。
**這件事沒有辦法防止**，跟你用 Cloudflare、Google 還是別的服務完全無關。

那些網路上教的方法都沒有用：

| 方法 | 為什麼沒用 |
|---|---|
| 停用右鍵 | 按 Ctrl+U 或 F12 一樣看得到 |
| 偵測有沒有開開發者工具 | 繞得過去，而且會誤判正常使用者 |
| 把程式碼弄得很難讀（混淆） | 只是增加麻煩，擋不住有心人；還會讓自己以後很難修 |

### 那怎麼辦？

**看情況，多數時候你其實不需要擔心：**

- **一般的教學工具、練習題、班網** —— 程式碼被看到沒有損失。真正有價值的是你的教學設計，不是那幾行 JavaScript。

**真的需要保護的東西，做法是「根本不要送出去」：**

- **測驗的答案** → 不要把答案寫在網頁裡。做法是：學生按下送出，網頁把答案傳回伺服器，伺服器比對後只回「對」或「錯」。這樣答案從頭到尾沒有離開過伺服器。
- **不想被看到的資料** → 同上，放在資料庫裡，網頁只拿它需要的那一小塊。
- **不想被別人隨便看到整個網站** → 用展示中心的**權限設定**：改成「需要密碼」或「不公開」。這是控制**誰能進來**，不是控制他進來之後能看什麼。

**一句話**：能防的是「誰進得來」，不能防的是「進來之後看到什麼」。
所以重要的東西不要放進網頁，要放在伺服器那一邊。

需要做這種「答案不外流」的設計時，跟 AI 說：

> 這個測驗的答案不可以出現在網頁的程式碼裡。
> 請把答案存在伺服器端，學生送出作答後由伺服器比對，只回傳對或錯。

## 有一種「一半」的做法：讓複製回去的檔案跑不起來

上面講的是「看得到」——那個防不了。但**「看到了、複製回去也不能用」是做得到的**。

指令：

```
node bin/hub.mjs lock 要部署的專案/我的測驗
```

它會在你的網頁裡加一段檢查：**這個頁面不是從自己的網址載入時就不執行**，
改成顯示一句「請從原本的網址開啟這個頁面」。

| | |
|---|---|
| ✅ 擋得住 | 把整包複製回去、雙擊 `index.html` 打開（這是最常見的情況） |
| ✅ 擋得住 | 放到別人自己的網域上 |
| ❌ 擋不住 | 看得懂那段程式碼、把它刪掉的人 |
| ❌ 擋不住 | 原始碼還是看得到，F12 一樣看得到 |

本機測試（localhost）不受影響，你自己開發不會被鎖到。
要拿掉就加 `--remove`。改完**要重新部署才會生效**。

**這是嚇阻，不是保護。** 它讓「隨手複製一份」變得不會成功，但真的想拆的人拆得開。
所以測驗答案這類東西**仍然不可以放在網頁裡**——那要用上面說的伺服器做法。

---

# 詳細說明

以上都是概要。以下是背後的實際限制，判斷邊界案例時用。

## 一、判準：這個程式要碰誰的東西？

只碰「使用者在瀏覽器裡主動交給它的東西」→ 適合。
要碰使用者的檔案系統、別人的網站、外部串流、或大檔案 → 不適合。

### 第一級：完全適合（純前端，零設定）

互動工具、測驗、遊戲、班網、靜態教材頁。

**也包括「讀使用者檔案做處理」的工具**，只要處理邏輯能用 JavaScript 寫。

> 例：數位乙級 CPLD 查核程式讀的是 `.bdf`／`.qsf` 文字檔，2381 行的查核邏輯
> 全部只用 Python 標準函式庫（`re`／`json`／`collections`），沒有任何第三方套件。
> 移植成 JavaScript 之後不但能部署，而且**檔案根本不會離開學生的瀏覽器**，
> 比現在上傳到伺服器的架構更好。

### 第二級：適合，需要用 D1（有資料要存）

報名系統、投票、留言板、器材清冊、需要登入的內容。

| 限制 | 數字 |
|---|---|
| 資料庫 | 500 MB／個（免費方案） |
| 單列上限 | 2 MB |
| 單一 SQL 敘述上限 | 100 KB（所以位元組要切成 40 KiB 一段存） |
| 圖片實務上限 | 200 KB（壓縮後）→ 約 2,000～2,500 張 |
| 讀取／寫入 | 500 萬列／天；10 萬列／天 |

圖片存 D1 時的設計要求：**列表頁用小縮圖（20–50 KB，一段就夠），
點進詳細頁才載原圖**。否則列表頁一次 50 張圖 × 5 段 = 250 列，
雖然仍在配額內（約 2 萬次瀏覽／天），但沒有必要。

### 第三級：只能做下載頁

程式本體必須在使用者電腦上跑的：

- 桌面程式（Python GUI、`.exe`、安裝包）
- 瀏覽器擴充功能（必須注入到別人的網站裡，網頁做不到跨網域讀 DOM）
- 要抓外部串流的（CORS 擋死）
- 要用原生二進位套件的（pikepdf／Pillow／numpy 的 C 部分）

**做法**：說明頁 ＋ zip 下載，`project-hub.json` 的 `project_type` 設 `"other"`，
卡片會自動標「需下載安裝」。參考 `要部署的專案/影像編輯程式/`，整包 32 KB。

### 第四級：不適合這套系統

- 影片庫（單檔就超過所有免費方案的上限）→ YouTube
- 大量 PDF 教材（>500 MB）→ 學校雲端硬碟，D1 只存連結
- 需要 Postgres 特性的複雜查詢、全文檢索
- 需要長時間運算（Worker 有 CPU 時間上限）

## 二、為什麼不建議「D1 ＋ 外部檔案倉庫」的混合架構

技術上可行（Worker 用 `fetch` 打對方的 REST API 即可），但：

- Supabase 免費方案**閒置一週會被暫停**（2026-08-30 查證官方定價頁），
  單檔上限 50 MB、總容量 1 GB。
- 混合架構讓暫停**更難察覺**：網站正常開，只有圖片和 PDF 全部壞掉。
  觸發條件很好預測——寒暑假兩個月沒人看，開學第一天全部教材圖不見。
- 可以用 Cloudflare 的 Cron Trigger 每週保活，但那是多一個「壞掉時沒人會注意到」的零件。

**結論**：會做這種架構的人不需要這套系統。這套系統的價值在
「一個平台、一組帳號、三個指令」，混進第二個平台就破功了。
真的需要大檔，該回頭認真評估 R2（綁一次卡），或把檔案放 YouTube／學校雲端硬碟。

## 三、原始碼保護的三級與真相

2026-08-08 的架構決策，原文保留：

| 級別 | 做法 | 實際效果 | 代價 |
|---|---|---|---|
| L1 一般 | Private Repo → production build → minify → 關閉 source map | 訪客看不到原始專案結構、檔名、註解 | 無 |
| L2 保護 | L1 ＋ mangle ＋ 選用性混淆 | 提高閱讀與還原成本 | **檔案變大、執行變慢、出錯無法 debug** |
| L3 真正保密 | 核心邏輯不進瀏覽器，改放 Worker API | **唯一真正有效** | 需改寫架構 |

**技術底線**：瀏覽器要執行的東西不可能真正隱形。
「看不到原始專案」做得到，「完全看不到瀏覽器正在執行的程式」做不到。
這與平台無關，Vercel／AWS／Azure 全部一樣。

### 現況說明（2026-08-30 實測）

**目前純靜態專案連 L1 都沒有做。** `hub ship` 對 `project_type: static` 的專案
不執行任何建置（見 `tools/build-plan.mjs`：「靜態網站不需要建置，直接上傳檔案」），
`public/` 底下的檔案是**逐字上傳**的。

實測 `要部署的專案/電阻識別測驗APP/public/script.js`：357 行，
中文註解完整保留，沒有壓縮。線上看到的就是這一份。

這不是缺陷，是取捨——加建置步驟就要求使用者的專案有 `package.json` 與
建置工具鏈，而這套系統的目標讀者是不會用終端機的老師。
**但這件事必須講清楚，不能讓人以為部署過就自動有保護。**

真正需要保護的邏輯，唯一有效的做法是 L3：放進 Worker，瀏覽器只拿得到結果。

## 四、事前約定的完整版規格

給有底子的人。分兩段：先問可行性，確認之後才給規格。

**第一段：先問可行性**

> 我要做一個 ⟨描述⟩，之後要部署到 Cloudflare Workers 的免費方案。
>
> **動手之前先回答我一件事：這個東西的核心工作，能不能只靠瀏覽器完成？**
>
> 判斷方式——它需不需要：讀寫使用者電腦上的檔案（不是透過檔案選擇對話框）／
> 進入別的網站抓內容／下載外部影音串流／用需要編譯的套件
> （ffmpeg、pikepdf、Pillow、numpy…）／存超過 2 MB 的單一檔案。
>
> **有任何一項是「需要」，就直接告訴我做不到，並說明是哪一項。**

**第二段：確認可行之後**

> 請照以下限制做：
>
> **架構**——純前端優先；需要伺服器邏輯時寫成 Cloudflare Worker（JS／TS），
> 不要用 Flask、Django、Express；要存資料就用 D1，不要用 Supabase、Postgres、
> Prisma；不要用需要編譯的原生套件；不要引入需要綁付款方式的服務
> （R2、Durable Objects）。
>
> **資料夾結構**
> ```
> 專案資料夾/
> ├─ public/          ← 網頁檔案全部放這裡
> │  ├─ index.html    ← 入口一定要叫這個名字
> │  ├─ css/
> │  └─ js/
> └─ 截圖.png          ← 首頁截圖放根目錄，不要放進 public/
> ```
> 入口一定要是 `public/index.html`，叫別的名字部署後會 404。
> 不要把設定檔、說明文件、名單、`.env` 放進 `public/`——那個資料夾會整包公開上網。
>
> **圖片**——要顯示的圖片壓到 200 KB 以內；使用者上傳的圖片在瀏覽器裡壓縮之後
> 才送出（`browser-image-compression`），iPhone 的 HEIC 用 `heic2any` 轉檔。
> 這是硬性限制，不是優化。
>
> **不要放進資料夾**——真實名單、成績、聯絡資料、教室現場照片
> （背景常夾帶資訊）、`.env`、金鑰。

## 五、事前約定省下的部署時間

| 沒先講，後來要處理 | 先講了 |
|---|---|
| 檔案散在根目錄 → 要跑 `hub new` 搬進 `public/`，還要逐項確認 | 結構本來就對，直接 `hub ship` |
| 入口叫 `我的程式.html` → 部署後 404，回頭改名重推 | 不會發生 |
| 用了 Flask／Next.js SSR → **整個要重寫** | 一開始就不會走那條路 |
| 圖片沒壓 → 存不進 D1，回頭加壓縮 | 一開始就有 |
| 名單、`.env` 混在裡面 → 部署前掃描擋下，逐項確認 | 不會混進去 |
| 沒有截圖 → 卡片空白，事後補 | 已經有了 |

最貴的是第三列（架構選錯要重寫），最常發生的是第一、二列。

## 六、事後偵測的極限

`hub detect` 可以從檔案特徵給建議：

| 看到什麼 | 判斷 |
|---|---|
| 根目錄有 `index.html`、沒有 `package.json` | 純靜態網站 |
| `package.json` 有 build script ＋ 框架 | 要建置的網站 |
| `manifest.json` 含 `content_scripts`／`host_permissions` | 瀏覽器擴充功能 → 下載模式 |
| `install.ps1`／`安裝.bat`／`setup.py`／`*.exe` | 有安裝流程 → 下載模式 |
| import 含 `tkinter`／`PyQt`／`wx` | 桌面 GUI → 下載模式 |
| `requirements.txt` 含 Flask／Django／uvicorn | 伺服器程式 → 要人判斷 |
| `Cargo.toml`／`*.sln`／`pom.xml` | 編譯型程式 → 下載模式 |

**但偵測有天花板。** PDF 處理工具與 CPLD 查核程式的檔案特徵幾乎一樣
（都是 Python、都讀使用者的檔案），但一個要重寫、一個不用——
差別在**用到的函式庫搬不搬得動**（pikepdf／Pillow 有 C 擴充，`re`／`json` 沒有），
那要讀程式碼才知道。

所以偵測能做到的是「這看起來是要在你電腦上跑的程式，預設走下載模式；
如果你認為它可以改成網頁，告訴我」——把球明確交給人，而不是猜。
